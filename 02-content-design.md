# 内容设计与协议

## 内容身份

使用 `content/posts/<id>/zh.md` 和可选的 `en.md`。目录标识是文章永久链接及译文关联的基础，每种语言最多一个版本。图片与文章保存在同一目录。

用途和呈现方式分开：`kind` 表达文章、日记或学习笔记；`layout` 表达文字、图文或画廊。它们共用同一内容集合，不建立互不兼容的五套数据来源。

## Frontmatter

| 字段 | 规则 |
| --- | --- |
| `title` | 必填，非空标题 |
| `publishedAt` | 必填，有效日期；用于展示和排序 |
| `kind` | `article` / `diary` / `note` |
| `layout` | `text` / `illustrated` / `gallery` |
| `tags` | 至少一个非空标签；禁止路由歧义字符 |
| `summary` | 可选；非空时优先使用 |
| `cover` / `coverAlt` | 可选的本地封面及替代文字 |
| `draft` | 可选布尔值，默认 false；控制公开状态 |
| `featured` | 可选布尔值，默认 false |
| `pinnedOrder` | 可选非负整数，小值排前；同值按日期倒序 |
| `demo` | 可选布尔值；标记演示内容 |

schema 位于 `src/content.config.ts`。构建前文件校验额外检查标识冲突、图片存在性和画廊非空，并提供具体文件位置。

## 派生数据

构建时生成摘要、首图回退、目录、卡片数据、公开排序、分页、标签集合、月份归档和译文关系。摘要自动提取最多 160 个字符，剔除图片和代码块；没有正文文字时为空。

封面优先使用指定资产，否则选择正文首图。页面通过 Astro 图片处理获得静态优化资产，浏览器不承担原图转换。

## 路由与语言

- 首页：`/zh/`、`/en/`；后续分页为 `/zh/page/2/`。
- 文章：`/<locale>/posts/<id>/`。
- 类型：`/<locale>/types/<kind>/`；标签：`/<locale>/tags/<tag>/`，需要时继续 `/page/<n>/`。
- 同一内容的公开译文存在时直接关联；缺失时不生成替代译文页面。
- 列表、搜索、RSS 只包含当前语言的公开版本。标签名称分别由对应语言内容提供。

## 草稿与发布

开发模式可以预览草稿，部署构建从所有公开入口排除草稿。产物检查同时验证 HTML、订阅、Sitemap、链接和草稿标记。`publishedAt` 不提供定时发布，未来日期不会代替 `draft` 控制。

实际写作步骤见 [内容维护指南](docs/content/authoring.md)。
